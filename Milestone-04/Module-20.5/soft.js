const person = ['rakib','nokib','shakib','akib']

const shortedPerson = person.sort();
console.log(shortedPerson);

const number = [4,2,6,3,7,3,9]
/**
 * kibabe aker odik array ke ak shathe shajabo.
 * 1.sort (diye)
 * Ascending ---> smaller to large
 * Decending ---> large to smaller
 * 
 */

const number_asc = number.sort();
console.log(number_asc);